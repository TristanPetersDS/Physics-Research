# Python analysis stack for the neutrino-directionality work.
#
# Verified-working combo (2026-06, Python 3.14.3, Open MPI 5.0.10, TeX Live 2026):
#   numpy 2.4.2   scipy 1.17.1   matplotlib 3.10.9
#   pandas 3.0.1  tqdm 4.68.1    mpi4py 4.1.2
#
# Pins below are floors (>=). For an exact, byte-for-byte reproduction of the
# verified environment, change every >= to == .
#
# NOT installable via pip (checked by setup.sh):
#   * mpi4py compiles against a SYSTEM MPI — `mpiexec` must already exist.
#   * the plot scripts set text.usetex=True and need a LaTeX toolchain (pdflatex).

numpy>=2.4.2
scipy>=1.17.1
matplotlib>=3.10.9
pandas>=3.0.1
tqdm>=4.68.1
mpi4py>=4.1.2
